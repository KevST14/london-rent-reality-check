# Runtime dependencies for the Streamlit Cloud deploy of app/streamlit_app.py.
# Local development uses uv + pyproject.toml + uv.lock instead of this file.
# Kept lean on purpose: no lightgbm, jupyter, osmnx or playwright (the app does
# not import them), so the deploy stays small and fast.
streamlit>=1.37
streamlit-folium>=0.20
folium>=0.16
pandas>=2.2
numpy>=1.26
pyarrow>=16
scikit-learn>=1.5
shap>=0.46
geopandas>=1.0
shapely>=2.0
matplotlib>=3.9
joblib>=1.3
